version: "3.8"

services:
  mongodb:
    image: mongo:7.0   # ← NOT latest
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: always   # ← important

volumes:
  mongo-data: